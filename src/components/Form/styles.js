import { makeStyles } from "@mui/styles";

export default makeStyles((theme) => ({
    paper: {
        padding: "20px",
    },
    form: {
        display: "flex",
        flexWrap: "wrap",
        justifyContent: "center",
    },
    input: {
        marginBottom: 20,
    },
    fileInput: {
        width: "100%",
        margin: "10px 0",
    },
    button: {
        marginBottom: 10,
    },
}));
