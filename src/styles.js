import { makeStyles } from "@mui/styles";

export default makeStyles((theme) => ({
    appBar: {
        borderRadius: 8,
        margin: "30px 0",
        display: "flex",
        justifyContent: "center",
        alignItems: "center",
    },
    heading: {
        marginRight: "20px",
        color: "#202B2C",
    },
    image: {
        marginLeft: "20px"
    },
}));
